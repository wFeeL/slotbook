services:
  migrate:
    build: ./backend
    env_file: ./backend/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://booking:booking@postgres:5432/booking
    depends_on:
      postgres:
        condition: service_healthy
    command: uv run alembic upgrade head
    restart: "no"
    volumes:
      - ./backend:/app
      - /app/.venv

  api:
    build: ./backend
    env_file: ./backend/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://booking:booking@postgres:5432/booking
      TEST_DATABASE_URL: postgresql+asyncpg://booking:booking@postgres:5432/booking_test
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir app
    volumes:
      - ./backend:/app
      - /app/.venv

  bot:
    build: ./backend
    env_file: ./backend/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://booking:booking@postgres:5432/booking
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      api:
        condition: service_started
    command: uv run python main_bot.py
    ports:
      - "8001:8001"
    volumes:
      - ./backend:/app
      - /app/.venv

  worker:
    build: ./backend
    env_file: ./backend/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://booking:booking@postgres:5432/booking
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    command: uv run python main_worker.py
    restart: unless-stopped
    volumes:
      - ./backend:/app
      - /app/.venv

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: booking
      POSTGRES_USER: booking
      POSTGRES_PASSWORD: booking
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/postgres/init-test-db.sql:/docker-entrypoint-initdb.d/init-test-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U booking -d booking"]
      interval: 5s
      timeout: 5s
      retries: 10

  miniapp:
    build:
      context: ./miniapp
      args:
        VITE_API_BASE_URL: ""
    depends_on:
      api:
        condition: service_started
    ports:
      - "5174:80"

volumes:
  postgres_data:
